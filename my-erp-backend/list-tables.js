const {Pool} = require('pg'); const pool = new Pool({host:'localhost',port:5432,user:'postgres',password:'Zillion',database:'goldenfield_erp'}); pool.query("SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' ORDER BY table_name").then(r => { console.log(r.rows.map(x => x.table_name)); pool.end(); }).catch(e => { console.error(e); pool.end(); process.exit(1); })
